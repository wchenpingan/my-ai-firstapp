import streamlit as st

st.set_page_config(
    page_title="我的 AI 全能工作台",
    page_icon="👋",
)

st.title("👋 欢迎来到 AI 全能工作台")

st.markdown("""
### 这里集成了我开发的所有 AI 工具：

- **💬 智能对话**: 类似于 ChatGPT 的通用聊天机器人。
- **🔥 文案生成**: 专为小红书/朋友圈打造的爆款写作助手。
- **📊 数据分析**: 上传 PDF 或 Excel，AI 帮你自动分析。

👈 **请在左侧侧边栏选择你需要的功能**
""")

st.info("💡 提示：在下方输入一次 API Key，所有工具都能自动使用！")

# --- 全局 API Key 管理 ---
# 我们把 Key 存到 session_state 里，这是一个"全局变量"，就像浏览器的 Cookie 一样
# 只要浏览器不关，所有页面都能读到这个 Key
key = st.text_input("请输入 DeepSeek API Key", type="password")

if key:
    st.session_state["api_key"] = key
    st.success("✅ Key 已保存！现在去点击左侧的工具吧！")

# 如果 secrets 里有，也自动加载（方便你自己测试）
if "DEEPSEEK_API_KEY" in st.secrets:
    st.session_state["api_key"] = st.secrets["DEEPSEEK_API_KEY"]
